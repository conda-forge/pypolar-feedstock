{% set name = "pypolar" %}
{% set version = "1.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pypolar-{{ version }}.tar.gz
  sha256: bce783856e64a5000be6f00e0890566ada7af043c849317334d7a6eb889c3beb

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools >=44
  run:
    - matplotlib-base
    - numpy
    - python >={{ python_min }}
    - sympy
    - scipy
    - pytest
    - pyyaml

test:
  imports:
    - pypolar
  requires:
    - pytest
    - pip
    - python {{ python_min }}
  source_files:
    - tests/test_jones.py
    - tests/test_stokes.py
  commands:
    - pip check
    - pytest tests/test_jones.py
    - pytest tests/test_stokes.py

about:
  home: https://github.com/scottprahl/pypolar.git
  summary: Python module for polarizers and birefringent elements with support for ellipsometry.
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE.txt
  doc_url: https://pypolar.readthedocs.io/
  dev_url: https://github.com/scottprahl/pypolar

extra:
  recipe-maintainers:
    - scottprahl
